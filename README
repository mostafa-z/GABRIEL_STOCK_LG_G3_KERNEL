Gabriel kernel
==============

This file was moved to Documentation/README

This source is used for LGe G3 android device with 3.4.x kernel version.

Here you can find out many branches builded for testing purpose,
the "stockbase" branch is stable in many usage pattern.

Please see "default_build.sh" for build instruction,
use "./default_build.sh" to start build script.
